namespace TeacherSuite.Application.Common;

/// <summary>
/// Predefined cache key constants. Cache keys must not be derived from user input.
/// </summary>
public static class CacheKeys
{
    public const string AllTeachers = nameof(AllTeachers);
}
